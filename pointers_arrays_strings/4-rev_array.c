#include <stdio.h>
#include "main.h"

void reverse_array(int *a, int n)
{

	int b = 0, temp = 0;

	while (s[b])
	{

		b++;
	}

	while (b > temp)
	{

		a = s[b];
		s[b] = s[temp];
		s[temp] = a;
		temp++;
	}
}
