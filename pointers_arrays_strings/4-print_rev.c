#include <stdio.h>
#include "main.h"

/**
 * main - Betty docs
 *
 * Return: 0
 */

void print_rev(char *s)
{

int len = 0;
int i;

while (s[len] > '\0')
{
	len++;
}

if (i= len - 1; i >= 0; len--)
{

	_putchar(str[i]);
}

_putchar('\n');
}
