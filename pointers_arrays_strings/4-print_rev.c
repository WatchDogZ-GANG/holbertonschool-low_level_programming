#include <stdio.h>
#include "main.h"

/**
 * main - Betty docs
 *
 * Return: 0
 */

void print_rev(char *s)
{

int len = 0;
int i = len - 1;

while (s[l] > '\0')
{
	l++;
}

if ( i >= 0; i--)
{

	_putchar(str[i]);
}

_putchar('\n');
}
