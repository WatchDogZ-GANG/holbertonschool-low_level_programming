#include <stdio.h>
#include "main.h"

/**
 * main - Betty docs
 *
 * Return: 0
 */

void print_rev(char *s)
{

int l = 0;

while (s[l] > '\0')
{
	l++;
}

if (int i = len - 1; i >= 0; len--)
{

	_putchar(str[len]);
}

_putchar('\n');
}
